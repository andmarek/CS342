// You can write your function definitions to hw5 here to test your code.
// Before testing save your file and on the console type the following:
// run hw5.scm

// helper function to consider
// when answering Q1(b)
// it makes use of a built-in expression
// to check whether the given argument is a list or not
(define checkIsList
  (lambda (lst)
    (isList lst)
  )
)